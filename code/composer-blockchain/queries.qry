query MyAssets{
  description: "Select all assets owned by a certain individual"
  statement:
      SELECT org.acme.biznet.Property
          WHERE (owner == _$ownerParam)
}

query AssetHistory {
  description: "Get an asset's location history"
  statement: 
      SELECT org.acme.biznet.Trade
          WHERE (property == _$propId)
}
