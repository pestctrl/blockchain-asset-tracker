<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BlockchainApp"
             x:Class="BlockchainApp.PackagesPage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <!--<Frame Padding = "10" VerticalOptions="Start" BackgroundColor="#555">
                <Label x:Name="welcomeMessage" TextColor="#ddd" VerticalOptions="Start" FontSize = "20" HorizontalTextAlignment="Center" Text="User Name" AutomationId="welcome"/>
            </Frame>-->

            <ListView x:Name="listView" ItemSelected="Selected_Handler">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal">
                                <Switch IsToggled="False" x:Name="isToggled"></Switch>
                                <Label Text="{Binding packageId}" TextColor="#611" VerticalOptions="Center"></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <Frame Padding = "0" VerticalOptions="End" BackgroundColor="Gray">
                <StackLayout Orientation="Horizontal" HorizontalOptions="Center" >
                    <Button x:Name="create_package" BackgroundColor="#eee" TextColor="#333" Text="Create Package" FontSize="10" WidthRequest="80" Clicked="CreatePackage"/>
                    <Button Text="Send Package" BackgroundColor="#eee" TextColor="#333" FontSize="10" WidthRequest="80"/>
                </StackLayout>
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>